{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",

  "Database": {
    "_comment": "Empty by default so a fresh clone runs on the in-memory repository without installing SQL Server. Set in appsettings.Development.json locally, and as an App Service connection string in Azure.",
    "ConnectionString": "",
    "InitializeSchema": true
  }
}
